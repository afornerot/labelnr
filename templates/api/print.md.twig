{{level}}# Synthèse

**TOTAL = {{ score|number_format(0, '.', '') }}**

| Code | Libellé | Score |
| --- | --- | ---: |
{% set currentthematic = "" %}
{% for pa in pas %}
{% if currentthematic != pa.thematic.code %}
{% if not loop.first %}
|  |  |  |
{% endif %}
| **{{ pa.thematic.code }}**| **{{ pa.thematic.title }}** | **{{ pa.thematic.score|number_format(0, '.', '') }}** |
{% set currentthematic = pa.thematic.code %}
{% endif %}
| {{ pa.code }} | {{ pa.title }} | {{ pa.score|number_format(0, '.', '') }} |
{% endfor %}

{% for pa in pas %}
{% for tir in pa.tirs %}
<div class='break-page'></div>

{{level}}# {{ tir.code }} : {{ tir.title }}

**Maturité** : {{ tir.maturity.value }} ({{ tir.maturity.title }})

{% if tir.comment %}

{{ ("\n" ~ tir.comment)|replace({"\n#": "\n" ~ level ~ "#"})|replace({"\n": "\n\n"})|trim }}
{% endif %}

{{level}}## D/M/R

{% if tir.dmrs|filter(d => d.type.value == 'documentation')|length > 0 %}
**Documentation**

{% for doc in tir.dmrs|filter(d => d.type.value == 'documentation') %}
- {{ doc.comment|replace({"\n":"\n- "}) }}
{% endfor %}
{% endif %}

{% if tir.dmrs|filter(d => d.type.value == 'moyen')|length > 0 %}
**Moyen**

{% for doc in tir.dmrs|filter(d => d.type.value == 'moyen') %}
- {{ doc.comment|replace({"\n":"\n- "}) }}
{% endfor %}
{% endif %}

{% if tir.dmrs|filter(d => d.type.value == 'resultat')|length > 0 %}
**Résultat**

{% for doc in tir.dmrs|filter(d => d.type.value == 'resultat') %}
- {{ doc.comment|replace({"\n":"\n- "}) }}
{% endfor %}
{% endif %}

{% endfor %}
{% endfor %}